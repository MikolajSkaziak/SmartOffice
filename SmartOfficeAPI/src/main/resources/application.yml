spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/smartoffice
    username: office_user
    password: office_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081

kind: Application

